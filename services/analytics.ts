/**
 * Analytics stub for tutorial and future events.
 * Replace with real analytics (e.g. Firebase, Mixpanel) when ready.
 */
export const trackTutorialEvent = (event: string): void => {
  console.log('Tutorial event:', event);
};
