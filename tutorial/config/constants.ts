/**
 * Tutorial versioning constants.
 * Bump TUTORIAL_VERSION when tutorial content changes; users who already
 * completed the previous version will see the tutorial again.
 */
export const TUTORIAL_VERSION = '1';
export const TUTORIAL_STORAGE_KEY = `hasSeenTutorial_v${TUTORIAL_VERSION}`;
